# Order Service

Order Service — это микросервис для управления заказами, разработанный на Java 17 с использованием Spring Boot 3.2. Проект включает полноценный CI/CD пайплайн с тестированием, покрытием кода, проверкой стиля, сканированием безопасности и сборкой Docker образа.

---

## 📦 Технологии

- **Java 17**
- **Spring Boot 3.2**
- **Spring Data JPA**
- **PostgreSQL**
- **H2** (для тестов)
- **Maven** (сборка и управление зависимостями)
- **JUnit 5 + Mockito** (тестирование)
- **JaCoCo** (покрытие кода)
- **Checkstyle** (стиль кода)
- **Allure** (отчеты по тестам)
- **SonarQube** (статический анализ кода)
- **Docker** (сборка и деплой)
- **OWASP Dependency-Check** (проверка уязвимостей зависимостей)
- **Trivy** (сканирование Docker образов)

---

## 🧪 CI/CD Пайплайн

Проект настроен для **GitLab CI/CD** с многоступенчатым пайплайном:

1. **Build** — компиляция проекта.
2. **Checkstyle** — проверка стиля кода.
3. **Test** — запуск модульных тестов с генерацией `allure-results` и `jacoco.exec`.
4. **Coverage** — генерация отчета JaCoCo.
5. **SonarQube** — статический анализ кода.
6. **Docker Build** — сборка Docker образа и пуш в GitLab Registry.
7. **Security (Java)** — OWASP Dependency-Check проверяет зависимости на уязвимости.
8. **Security (Docker)** — Trivy проверяет Docker образ на критичные уязвимости.
9. **Deploy** — ручной деплой Docker образа на продакшн сервер.

**Оптимизации пайплайна:**
- Кэширование Maven репозиториев и Docker слоёв.
- `interruptible: true` для всех долгих стадий.
- `needs` для минимизации повторных сборок.
- Отчеты (`JaCoCo`, `Allure`, `Checkstyle`, `Dependency-Check`, `Trivy`) сохраняются как артефакты на неделю.